<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<title>Employee Report - FreshMart</title>
	<link rel="stylesheet" href="/static/css/style.css" />
	<style>
		:root {
			--primary: #2ecc71;
			--secondary: #27ae60;
			--bg: #f7fbf8;
			--card: #ffffff;
			--muted: #6e7a70;
			--radius: 12px;
			--shadow: 0 6px 20px rgba(0, 0, 0, 0.05);
			--gradient: linear-gradient(135deg, var(--primary), var(--secondary));
		}

        body {
        font-family: "Segoe UI", Roboto, system-ui, -apple-system, "Helvetica Neue", Arial;
        background:
            repeating-linear-gradient(
            135deg,
            rgba(30, 60, 40, 0.08),
            rgba(30, 60, 40, 0.08) 20px,
            rgba(20, 40, 25, 0.03) 20px,
            rgba(20, 40, 25, 0.03) 40px
            );
        background-color: #f8fbf9;
        color: #222;
        margin: 0;
        padding: 2rem 1rem;
        -webkit-font-smoothing: antialiased;
        }

		.report-container {
			max-width: 1300px;
			margin: 0 auto;
			display: flex;
			flex-direction: column;
			gap: 1.5rem;
		}

		/* HEADER */
		.title {
			display: flex;
			align-items: center;
			gap: 1rem;
		}

		.brand-pill {
			background: var(--gradient);
			color: white;
			padding: 0.55rem 0.8rem;
			border-radius: 10px;
			font-weight: 700;
			box-shadow: 0 6px 18px rgba(46,204,113,0.18);
			font-size: 0.95rem;
		}

		h1 {
			margin: 0;
			font-size: 1.6rem;
			color: #1f7a44;
		}

		.subtitle {
			color: var(--muted);
			font-size: 0.95rem;
		}

		/* CARD STYLING */
		.card {
			background: var(--card);
			border-radius: var(--radius);
			padding: 1.25rem;
			box-shadow: var(--shadow);
			border: 1px solid rgba(15,20,10,0.03);
		}

		/* KPI SECTION */
		.kpi-section {
			display: flex;
			flex-wrap: wrap;
			gap: 1rem;
		}

		.kpi-card {
			flex: 1;
			min-width: 250px;
			padding: 1rem;
			border-radius: var(--radius);
			background: linear-gradient(180deg, rgba(255,255,255,0.95), rgba(255,255,255,0.8));
			box-shadow: 0 2px 8px rgba(15,20,10,0.05);
			text-align: center;
			border: 1px solid rgba(0,0,0,0.03);
			transition: transform 0.18s ease, box-shadow 0.18s ease;
		}

		.kpi-card:hover {
			transform: translateY(-4px);
			box-shadow: 0 10px 26px rgba(15,20,10,0.08);
		}

		.kpi-card h3 {
			color: #1f7a44;
			margin-bottom: 0.6rem;
			font-size: 1rem;
		}

		.kpi-card p {
			margin: 0.2rem 0;
			font-size: 1rem;
			color: #333;
		}

		.kpi-card:nth-child(1) { border-top: 4px solid #2ecc71; }
		.kpi-card:nth-child(2) { border-top: 4px solid #f1c40f; }
		.kpi-card:nth-child(3) { border-top: 4px solid #3498db; }

		/* FILTERS */
        .filter-section {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
        gap: 1rem;
        align-items: end;
        }

        .filter-section > div {
        min-width: 140px;
        max-width: 220px;
        }

        .filter-section > div:nth-child(4) {
        margin-left: -5rem;
        }

        #job_title {
        max-width: 160px;
        }

		.filter-section label {
			display: block;
			font-weight: 600;
			font-size: 0.9rem;
			margin-bottom: 0.3rem;
			color: var(--muted);
		}

		.filter-section input,
		.filter-section select {
			width: 100%;
			padding: 0.55rem 0.7rem;
			border-radius: 8px;
			border: 1px solid #dfe7df;
			font-size: 0.9rem;
			background: white;
			transition: all 0.2s ease;
		}

		.filter-section input:focus,
		.filter-section select:focus {
			border-color: var(--primary);
			box-shadow: 0 0 0 2px rgba(46,204,113,0.15);
			outline: none;
		}

		.filter-group {
			display: flex;
			align-items: center;
			gap: 0.4rem;
		}

		/* BUTTONS */
		.btn {
			cursor: pointer;
			padding: 0.6rem 1rem;
			border: none;
			border-radius: 8px;
			font-size: 0.9rem;
			font-weight: 600;
			transition: all 0.2s ease;
			display: inline-flex;
			align-items: center;
			gap: 0.4rem;
		}

		.btn.primary {
			background: var(--gradient);
			color: white;
			box-shadow: 0 6px 20px rgba(46,204,113,0.15);
		}

		.btn.primary:hover {
			transform: translateY(-1px);
			box-shadow: 0 8px 24px rgba(46,204,113,0.25);
		}

		.btn.secondary {
			background: #ecf5ef;
			color: #1f5135;
			border: 1px solid #d4e8da;
		}

		.btn.secondary:hover {
			background: #e4f3e9;
		}

		.action-buttons {
			display: flex;
			justify-content: flex-end;
			gap: 0.6rem;
		}

		/* TABLE */
		.report-table {
			width: 100%;
			border-collapse: collapse;
			border-radius: var(--radius);
			overflow: hidden;
			box-shadow: var(--shadow);
			font-size: 0.92rem;
		}

		.report-table th {
			background: linear-gradient(180deg, #2b6e3b, #2b6e3b);
			color: white;
			padding: 0.8rem;
			text-align: left;
			font-weight: 600;
			cursor: pointer;
			user-select: none;
		}

		.report-table td {
			padding: 0.75rem;
			border-bottom: 1px solid #f0f3f0;
		}

		.report-table tr:nth-child(even) {
			background: #fbfdfb;
		}

		.report-table tr:hover {
			background: #f1fbf5;
		}

		.sortable.asc::after { content: " ↑"; }
		.sortable.desc::after { content: " ↓"; }

		/* DROPDOWN + MINI BUTTONS */
		.dropdown { position: relative; width: 100%; }
		.dropdown-toggle {
			width: 100%;
			background: #fff;
			border: 1px solid #dfe7df;
			border-radius: 8px;
			padding: 0.6rem;
			text-align: left;
			cursor: pointer;
		}
		.dropdown-menu {
			display: none;
			position: absolute;
			background: white;
			border: 1px solid #dfe7df;
			border-radius: 8px;
			width: 100%;
			margin-top: 5px;
			box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
			z-index: 999;
			padding: 0.6rem;
		}
		.dropdown.open .dropdown-menu { display: block; }

		.dropdown-item {
			display: flex;
			align-items: center;
			gap: 8px;
			padding: 0.4rem 0.5rem;
			border-radius: 6px;
			cursor: pointer;
		}

		.dropdown-item:hover { background: #e8f9ee; }

		.btn-mini {
			border: none;
			border-radius: 6px;
			padding: 0.3rem 0.7rem;
			font-size: 0.8rem;
			cursor: pointer;
			font-weight: 600;
		}
		.btn-mini.success { background: #e8f9ee; color: var(--secondary); }
		.btn-mini.danger { background: #fdeaea; color: #c0392b; }
	</style>
</head>

<body>
	<div class="report-container">
		<!-- Header -->
		<div class="title" style="justify-content: space-between; align-items: center;">
			<div style="display: flex; align-items: center; gap: 1rem;">
				<a href="/admin" class="btn secondary">← Back</a>
				<div class="brand-pill">FreshMart</div>
			</div>
			<div>
				<h1>Employee Report</h1>
				<div class="subtitle">Performance and tenure insights across departments.</div>
			</div>
		</div>

		<!-- KPI Cards -->
		<div class="card">
			<section class="kpi-section">
				<div class="kpi-card">
					<h3>Total Employees</h3>
					<p style="font-size:1.8rem; font-weight:700;">{{ employees|length }}</p>
				</div>
				<div class="kpi-card">
					<h3>Average Tenure</h3>
					<p style="font-size:1.8rem; font-weight:700;"><%= avg_tenure_years %> yrs</p>
				</div>
				<div class="kpi-card">
					<h3>Employee of the Month</h3>
					<% const top_emp = employees|sort(attribute='NumberOfSales', reverse=True)|first; %>
					<p><strong><%= top_emp.Name %></strong></p>
					<p class="muted"><%= top_emp.NumberOfSales %> Sales — <%= top_emp.DepartmentName %></p>
				</div>
			</section>
		</div>

		<!-- Filters -->
		<div class="card">
			<strong>Filters</strong>
			<section class="filter-section" style="margin-top: 0.8rem;">
				<div>
					<label>Department</label>
					<div id="employeeDepartmentDropdown" class="dropdown">
						<button type="button" class="dropdown-toggle">Select Department ▼</button>
						<div class="dropdown-menu">
							<div class="dropdown-actions" style="display:flex;justify-content:space-between;margin-bottom:0.4rem;">
								<button type="button" id="employeeSelectAllDepts" class="btn-mini success">Select All</button>
								<button type="button" id="employeeClearAllDepts" class="btn-mini danger">Clear</button>
							</div>
							<div class="dropdown-list">
								<% for (let dept of departments) { %>
									<label class="dropdown-item">
										<input type="checkbox" class="employee-dept-option" value="<%= dept %>">
										<%= dept %>
									</label>
								<% } %>
							</div>
						</div>
					</div>
					<input type="hidden" id="employeeDepartment" name="department" value="">
				</div>

				<div>
					<label>Name</label>
					<input type="text" id="name" name="name" placeholder="Search by name">
				</div>

				<div>
					<label>Job Title</label>
					<select id="job_title" name="job_title">
						<option value="">All</option>
						<% for (let title of job_titles) { %>
							<option value="<%= title %>"><%= title %></option>
						<% } %>
					</select>
				</div>

				<div>
					<label>Hire Date</label>
					<div class="filter-group">
						<input type="date" id="hire_date_from">
						<span>-</span>
						<input type="date" id="hire_date_to">
					</div>
				</div>

				<div>
					<label>Total Revenue ($)</label>
					<div class="filter-group">
						<input type="number" id="revenue_min" placeholder="Min">
						<span>-</span>
						<input type="number" id="revenue_max" placeholder="Max">
					</div>
				</div>
			</section>

			<div class="action-buttons" style="margin-top:1rem;">
				<button class="btn primary" id="filterBtn">🔍 Apply Filters</button>
			</div>
		</div>

		<!-- Table -->
		<div class="card">
			<table class="report-table">
				<thead>
					<tr>
						<th class="sortable" data-column="EmployeeID">Employee ID</th>
						<th class="sortable" data-column="Name">Name</th>
						<th class="sortable" data-column="DepartmentName">Department</th>
						<th class="sortable" data-column="JobTitle">Job Title</th>
						<th class="sortable" data-column="HireDate">Hire Date</th>
						<th class="sortable" data-column="TotalRevenue">Total Revenue</th>
						<th class="sortable" data-column="NumberOfSales">Number of Sales</th>
						<th class="sortable" data-column="AverageSaleValue">Average Sale Value</th>
					</tr>
				</thead>
				<tbody id="employeeTableBody">
					<% for (let emp of employees) { %>
					<tr>
						<td><%= emp.EmployeeID %></td>
						<td><%= emp.Name %></td>
						<td><%= emp.DepartmentName %></td>
						<td><%= emp.JobTitle %></td>
						<td><%= emp.HireDate %></td>
						<td>${{ '%.2f' | format(emp.TotalRevenue or 0) }}</td>
						<td><%= emp.NumberOfSales or 0 %></td>
						<td>${{ '%.2f' | format(emp.AverageSaleValue | float or 0) }}</td>
					</tr>
					<% } %>
				</tbody>
			</table>
		</div>
	</div>

	<script src="/static/js/main.js"></script>
</body>
</html>
